# RM-M2-MIG-02: versioned migration scripts live here as ????_*.sql.
# Generated by `make schema-generate` from schema/schema.yaml; the
# committed SQL is the build artefact, the YAML is the canonical
# source. Each file is embedded as a resource (see csproj) and
# applied in order by BessDbMigrator via DbUp. The first migration
# (0001_initial.sql) lands with RM-M2-MIG-03.
